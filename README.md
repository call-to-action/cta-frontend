# Call To Action
### Getting started
##### `react-native`
`$ brew install node`
`$ brew install watchman`
`$ npm install -g react-native-cli`
##### `cta-frontend`
`$ npm install`
`$ react-native run-ios`
